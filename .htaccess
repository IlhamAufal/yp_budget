<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep requests that are already inside public/ from being rewritten again.
    RewriteCond %{REQUEST_URI} !/public(?:/|$) [NC]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
